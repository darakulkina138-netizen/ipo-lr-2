#Переменной var_int присвойте значение 70, var_float - значение 8.4, var_str -"No".
var_int = 70
var_fload = 8.4
var_str = "No"
#Измените значение, хранимое в переменной var_int, увеличив его в 3.5 раза, результат свяжите с переменной big_int35
var_int = 70 * 3.5
big_int = var_int
#Измените значение, хранимое в переменной var_float, уменьшив его на единицу, результат свяжите с той же переменной.
var_float = 8.4 - 1
#Разделите var_int на var_float, а затем big_int35 на var_float. Результат данных выражений не привязывайте ни к каким переменным.
var_int / var_float
big_int35 / var_float
#Измените значение переменной var_str на "NoNoNoYesYes". При формировании нового значения используйте операции конкатенации (+) и повторения строки (*).
var_str = var_str * 3 + "Yes" * 2
#Выведите значения всех переменных с помощью одной функции
print(f"var_int = {var_int}, var_float = {var_float}, var_str = '{var_str}', big_int35 = {big_int35}")
